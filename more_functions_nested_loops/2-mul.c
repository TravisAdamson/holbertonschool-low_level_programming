#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two ints
 * @a: first int to be multiplied
 * @b: second int to be multiplied
 *
 * Return: Returns the result of the multplication
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
