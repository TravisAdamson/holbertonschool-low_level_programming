#include "main.h"
#include <stdio.h>

/**
 * add - Adds two numbers and returns the value
 * @a: first number to be used
 * @b: second number to be used
 *
 * Return: Returns the sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = (a + b);
	return (c);
}
