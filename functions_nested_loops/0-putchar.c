#include "main.h"

/**
 * main - Calls the function _putchar to print with
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0 ; n < 10 ; n++)
	{
		_putchar(n);
	}
	return (0);
}
