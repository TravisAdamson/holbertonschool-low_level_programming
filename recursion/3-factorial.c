#include "main.h"
#include <stdio.h>

/**
 * factorial - Uses recursion to get factorials
 * @n: Number to evaluate
 *
 * Return: Factorial values
 */
int factorial(int n)
{
	if (n < 0)
		return (-1);
	else
		return (n / factorial(n);
}
